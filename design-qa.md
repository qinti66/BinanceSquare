# Design QA — 墨金创作台

final result: passed

验收日期：2026-09-18。以下为初版设计还原记录；用户随后要求移除演示内容，当前版本验收见末尾“空白工作台更新”。初版范围：本机可运行应用、选定设计还原、演示发布与恢复流程。真实币安发布未实测，尚未提供真实 OpenAPI Key。

## Source and target

- 唯一视觉基准：`docs/design-reference.png`，用户选定的第 1 个 ImageGen 方案，1487 × 1058 px。
- 实现地址：http://localhost:4173 。本地 API 为 127.0.0.1:8787。
- 对比视口：1487 × 1058 CSS px，deviceScaleFactor = 1，Chrome，深色主题，帖子编辑器，演示账号 1、2 选中，示例内容与 K 线。
- 实现截图：`artifacts/browser-qa/desktop-final.png`。
- 全屏同图对比：`artifacts/browser-qa/comparison-final.png`，左侧原稿，右侧实现；缩略版为 `comparison-final.jpg`。
- 局部 1:1 对比：`artifacts/browser-qa/comparison-editor.jpg`，检查标题、正文、标签、K 线及右侧预览。
- 原稿、实现及上述对比图均已直接查看。另由独立审查代理查看最终对比图与手机截图。无未解决的 P0/P1/P2 视觉问题。

## Browser access

原内置浏览器启动被 Windows sandbox ACL 错误阻断。用户回复“可以的”后，按明确授权使用直接 Playwright 与本机 Chrome 完成验收。此前的 blocked 结论已由实际浏览器证据取代。

## Fidelity surfaces

1. 字体与层级：中文无衬线字体；桌面主标题 40 px、正文 20 px、内容标题 32 px。修正初版偏小的排版后，直接检查行距、换行与局部对比。
2. 间距与布局：保留左侧导航、中间编辑器、右侧账号与内容预览。最终文档尺寸恰为 1487 × 1058，无横向或额外整页溢出；编辑卡片约 779 × 880 px，K 线约 725 × 355 px。
3. 颜色与样式：深炭灰背景、暖黄色主操作、绿色/红色蜡烛、边框及圆角已与原稿同图检查。
4. 图片与图标：头像为独立 512 px 图像，图像均成功加载；品牌图使用设计原稿裁切资产；界面使用 Phosphor 图标；K 线由实际图表组件绘制。
5. 内容与状态：中文文案完整，账号显示名与平台身份分离；演示状态、真实发布确认、草稿状态、逐账号结果清楚可见。公共接口限制在产品中明确提示。

接受的 P3 差异：系统字体光栅差异、右侧约 12 px 边距差异、重新生成的头像照片、可交互图表的示例蜡烛及必要归属标识。演示发布标识用于如实说明行为。

## Comparison iterations

- 初版：`comparison-initial.jpg`。发现正文与预览字体偏小，桌面内容高度超过视口。
- 修正字体、面板最小高度与间距后，重新截取完整视图和编辑区局部对比，得到最终图。
- 360 px 窄屏回归发现工具栏横向溢出；调整小屏间距、折行与弹层位置后重新截图，横向滚动宽度等于视口。
- 手机滚动至底部，实测发布按钮在固定底栏上方完整可见并可打开确认弹窗。

## Interaction verification

`npm test`：36 项全部通过。包含 API、客户端恢复、静态构建路由、本地数据目录保护与存储故障恢复测试。

`npm run test:browser`：8 项全部通过，结果为 `artifacts/browser-qa/results.json`：

- 桌面无视口溢出，头像加载正常。
- 添加、编辑、删除账号，展示名称仅在本站使用，API Key 不以明文返回。
- 表情、话题、币种插入与刷新后的自动草稿恢复。
- 相同 K 线重复插入、两个账号模拟发布与历史记录。
- 草稿搜索、继续编辑与多草稿独立保存。
- 文章格式、封面及纯文本发布提示。
- 手机编辑、账号弹窗、导航与话题弹层。
- 360、768、1024、1280 px 响应式无横向溢出。

`npm run test:browser:media`：4 项全部通过，结果为 `artifacts/browser-qa/results-extended.json`：

- 小型 MP4 上传、自动封面与时长、刷新恢复、模拟发布。
- 服务已接受发布但响应断开时，刷新后使用同一请求 ID 恢复，发布历史没有重复。
- 已有草稿清空后保存、刷新仍为空。
- 手机底部发布操作完整可见且可点击。

核心流程无控制台或页面错误。扩展流程唯一控制台错误为测试主动制造的 ERR_CONNECTION_RESET；这是断网恢复检查的预期输入，无未处理的页面错误。

浏览器测试使用隔离的 5175/8788 端口与 `artifacts/browser-qa/data-*` 测试目录，禁止真实币安上游请求，不修改用户 `.data` 数据。

## Functional and security fixes verified

- 本地 `.data` 目录被 Vite 静态文件规则拒绝访问；普通、编码、大小写及 /@fs 变体检查通过。
- Windows 短暂文件占用会有限重试原子写入。首次落盘失败回滚未发送任务；中途失败保留已发送结果并停止剩余账号；重试不会重复向上游发布。
- 草稿版本与删除标记、浏览器独立备份、上传所属草稿、确认框忙碌状态、待恢复发布请求均有相应回归覆盖。
- 生产构建通过。当前本地服务已重启加载最终后端修复。

## Scope and limits

- 可在账号管理添加币安广场 OpenAPI Key。密钥在本机加密保存；真实线上接口仍需用户账号权限与实际请求验证。
- K 线作为图片发布；公开接口没有已核实的原生可跳转 K 线、投票、资产或盈亏组件发布字段。
- 富文本格式保存在草稿中，发布正文使用接口支持的纯文本。
- 历史记录仅包含本工作台发起的发布。
- 当前交付为本机应用，未部署公网服务。

final result: passed

## 空白工作台更新（当前版本）

按用户要求去除演示账号、预填内容与标签、示例 K 线、演示模式入口和模拟发布历史。保留原有深炭灰/暖黄色视觉方向与编辑器布局；与原始设计图之间的内容差异为本次明确要求。账号展示使用实际添加的账号。K 线只读取公开行情，失败时提示重试。

已直接查看生产页面截图 `artifacts/production-qa/clean-workspace.png`（1487 × 1058）和 `clean-workspace-mobile.png`（390 px 视口全页）：输入区为空、账号数为 0、未选账号时发布按钮禁用；排版完整，桌面和手机无横向溢出。手机底栏随视口固定，底部发布按钮滚动后可见并可操作。此范围无未解决的 P0/P1/P2 视觉问题。

验证结果：56 项自动化测试、8 项基础浏览器检查、4 项媒体/恢复检查、6 项生产 HTTP 浏览器检查全部通过；构建通过。检查涵盖旧浏览器缓存清理、用户草稿保留、真实账号选择与确认发布、视频上传、响应中断后同一请求恢复。发布和行情使用隔离测试上游，未执行真实币安发布。媒体恢复测试主动制造的 ERR_CONNECTION_RESET 为预期输入，无未处理页面错误。

final result: passed
